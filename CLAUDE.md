Create an electron environment in which we will create a desktop application for which everything is included outside the back end folder whatever is outside the packet folder should be included in the desktop application

install all the dependencies

Try to run using npm start Cheque all the things all the errors

You have to create 1 detachable window for when we click the recording button in the print app It is shown in the UI the record is written on the right most bottom When you click it it will start recording the microphone and system module And after you click it it will create a detachable window Where is the code for detachable window @detacheble-windows.html This is the detachable photo it will hover over the screen adn its dragable , make it 70% transparent so thet use can see through it, It has some buttons one is for my you can disable enable my using that button 1 is ask button in which it will create a window just below it which is also with 70% transparency Hand button it will cancel the meeting it will end the meeting

I will add all my API's later

i have frontend and backend I want you to integrate both of them in the back and just create whatever it is in the front end and other services then the backend to connect it to the back end select and play not change the back end folder Could change things outside the back and forth whatever it is

Currently I'm using windows In the back end run the build .bat for Windows Running two times So that I can assure the server Are running end of that Try to debug and get the back end change the backend folder ever

Try to fix all the logical issues